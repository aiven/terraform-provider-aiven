name: Unit tests

on:
  pull_request:
  push:
    branches:
      - main
      - v*

permissions:
  contents: read

jobs:
  go_test:
    strategy:
      matrix:
        os: [ubuntu-latest]
    runs-on: ${{ matrix.os }}

    steps:
    - uses: actions/setup-go@v2
      with:
        go-version: 1.18
    - uses: actions/checkout@v2
    - run: make test-unit
